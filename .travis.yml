language: python
python:
  - "3.7.2"
branches:
  - master
# command to install dependencies
install:
  - "pip install PyQt5"
  - "pip install -r requirements.txt"
# command to run tests
script: pytest /code/PPTX_DataSet_Maker_main.py